#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"

if [[ -d "$ROOT/pocketfm-contest-forge" ]]; then
	APP="$ROOT/pocketfm-contest-forge"
else
	APP="$ROOT"
fi

cd "$APP"

npm run guard:repo-hygiene < /dev/null
npm run prepush < /dev/null
