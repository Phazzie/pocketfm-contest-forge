<!-- Created: 2026-05-26 06:16 -->

# Tracking

Use this file for active issues, review findings, risks, and decisions that need follow-up.

## Active Review Findings

| Status | Priority | Item                                                                                        | Source        | Next Action                                                                                                           |
| ------ | -------- | ------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------- |
| Done   | P2       | Default protagonist is derived from title words, producing "Crown weaponizes a crown."      | Review        | Added explicit protagonist input, safer fixture default, and sanity test.                                             |
| Done   | P3       | Mechanism validation checks count, not uniqueness.                                          | Review        | Validates unique mechanism IDs and rejects duplicate selections.                                                      |
| Done   | P3       | Root `node_modules/.package-lock.json` contains local macOS install state.                  | Review        | Root installed-tree metadata is no longer modified in this patch.                                                     |
| Done   | P3       | Root `package-lock.json` has accidental name casing churn.                                  | Local status  | Root package-lock is no longer modified in this patch.                                                                |
| Done   | P3       | Quality guards and CI need review-hardening before merge.                                   | PR #1 review  | Inline disable checks, type-escape scanning, UI probe/browser timeout validation, and pinned CI actions are hardened. |
| Done   | P3       | Contest freshness dates use locale formatting in Svelte template.                           | PR #15 review | Replaced locale date formatting with stable ISO date labels to avoid SSR/client hydration drift.                      |
| Done   | P3       | Registered `council-review` could run in fixture forge without a fixture input builder.     | PR #15 review | Fixture/demo forge now excludes the live-only council module and asserts fixture modules do not fail.                 |
| Done   | P3       | Changing contest lane could leave the idle UI showing the default medieval brief.           | PR #15 review | Route load now builds initial locked Story Studio runs for every curated brief and renders the selected lane.         |
| Done   | P3       | Trope quality gate scene terms were biased toward medieval fantasy.                         | PR #15 review | Trope quality review now merges shared scene/cost terms with genre-specific terms before accepting live output.       |
| Done   | P3       | Live Story Studio ignored selected mechanisms and spent provider calls on deselected tools. | PR #15 review | Runner now locks deselected live artifacts before provider execution and tests zero-call and one-call paths.          |
| Done   | P3       | Five sequential live provider calls could overrun deployment duration.                      | PR #15 review | Vercel max duration is explicit at 300 seconds and route-level provider/executor timeouts cap per-module hangs.       |
| Done   | P3       | Production smoke timed out `cold-open-lab` at the 45-second route budget.                   | Live smoke    | Raised the route-level xAI/executor budget to 55/56 seconds while staying below the 300-second function duration.     |
| Done   | P3       | 55/56-second module timeouts left too little all-timeout overhead before Vercel 504s.       | PR #16 review | Added a 285-second application budget with a 70-second remaining-time guard before each provider call.                |
| Done   | P3       | Production smoke reached `trope-mutation-lab` but rejected weak episode pressure.           | Live smoke    | Hardened `trope-mutation-lab.v2` so episode pressure must start with a repeat cue and carry concrete recurring cost.  |
| Done   | P3       | Production smoke rejected `cliffhanger-futures` payoff warnings without audience risk.      | Live smoke    | Hardened `cliffhanger-futures.v2` so payoff warnings must start with an explicit audience-risk prefix.                |
| Done   | P3       | Production smoke rejected a `trope-mutation-lab` mutation rule without a rule-change cue.   | Live smoke    | Hardened `trope-mutation-lab.v4` so mutation rules must include an explicit inversion/subversion cue word.            |

## Product Risks

| Status    | Risk                                            | Why It Matters                                                                     | Mitigation                                                                                                                                  |
| --------- | ----------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Done      | Heuristic creative output is user-facing.       | It can create false confidence and waste writer time.                              | Production route now renders `StoryStudioRun` artifacts and no longer centers fixture/demo forge prose.                                     |
| Done      | Story mechanisms are not first-class modules.   | New story tools will become switch cases and ad hoc UI rendering.                  | Four initial modules now run through registry and runtime schemas.                                                                          |
| Mitigated | Contest data can drift.                         | Bad rules create bad submissions.                                                  | Curated briefs now expose `retrievedAt`, `staleAfter`, and source warnings; automated research refresh remains future work.                 |
| Done      | Scoring is heuristic.                           | Numeric confidence may look more authoritative than it is.                         | Production route no longer shows heuristic readiness score meters; qualitative assessment comes from live `council-review` artifacts.       |
| Mitigated | No persistence/story bible yet.                 | Serial writing needs continuity tracking.                                          | Story-state contract exists; persistence remains a future storage decision.                                                                 |
| Open      | Current UI is demo-grade, not production-grade. | Writers need a memorable, usable studio experience.                                | Execute `docs/PRODUCTION_STORY_STUDIO_EXECUTION_PLAN.md` and rebuild toward the colorful Serial Story Studio direction in root `DESIGN.md`. |
| Mitigated | Full Story Studio live run can be slow.         | Sequential model calls can outlast a serverless request if timeouts are unbounded. | Selected mechanisms gate provider calls, per-module timeouts fail closed, and a remaining-time guard locks instead of risking a 504.        |

## Active Follow-Up

| Status | Item                                                                      | Next Action                                                                                                                                                                                       |
| ------ | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Done   | Fake-provider live module boundary is absent.                             | Added provider port, live executor, JSON repair, schema validation, prose gate, and fake-provider tests for `cold-open-lab`.                                                                      |
| Done   | Real Grok provider adapter is still absent.                               | Added xAI Responses API adapter behind the provider port with env validation, provider metadata, timeout handling, and secret-safe tests.                                                         |
| Open   | Story-state contract is in-memory only.                                   | Add persistence after storage choice is made.                                                                                                                                                     |
| Done   | Remaining fixture module prose still needs live quality review.           | `council-review` now has module-specific prompts, runtime schema, fake-provider tests, and a live quality gate.                                                                                   |
| Open   | Future multi-hour work needs stricter execution plans.                    | Use `.agent/PLANS.md` and the global `exec-plan` skill for complex autonomous runs.                                                                                                               |
| Done   | Type safety should enforce a literal no-`any` policy.                     | Removed the registry `any` escape; heterogeneous module lookup uses `unknown`, and `guard:type-escape-budget` allows zero approved `any`.                                                         |
| Done   | Main branch needs remote quality enforcement.                             | Branch protection requires PRs, conversation resolution, and the `Verify` status check.                                                                                                           |
| Open   | Low npm audit finding exists through SvelteKit cookie.                    | Track upstream fix; CI should fail on moderate and higher vulnerabilities.                                                                                                                        |
| Done   | Public MVP needs Vercel and Grok delivery plan.                           | Production smoke passed against `https://pocketfm-contest-forge.vercel.app` with `live-ai`, `xai`, `cold-open-lab.v2`, status `success`, and 4 accepted variants.                                 |
| Open   | CI UI smoke currently relies on preview build output.                     | Keep `npm run verify` before `npm run verify:ui` in CI or add an explicit build preflight.                                                                                                        |
| Open   | CI UI smoke must not leave server children running.                       | Keep process-group cleanup in `scripts/verify-ui.mjs` covered by local and remote smoke.                                                                                                          |
| Done   | Vercel project is not yet proven from public URL.                         | Production URL is deployed and `npm run deploy:readiness` passes against `https://pocketfm-contest-forge.vercel.app`.                                                                             |
| Done   | Vercel CLI is not authenticated in the local shell.                       | Vercel CLI is authenticated as `phazzie` and the local project link is active.                                                                                                                    |
| Done   | Real deployed Grok smoke is not yet proven.                               | `RUN_LIVE_AI_SMOKE=1 LIVE_AI_SMOKE_URL=https://pocketfm-contest-forge.vercel.app LIVE_AI_SMOKE_ACCESS_CODE=<configured access code> npm run ai:smoke` passed with winner `mara-vey-sisters-name`. |
| Open   | Full Story Studio deployed smoke is not yet proven after route migration. | Run `npm run ai:smoke` against production after deploying the `runLiveStudio` route; the script now expects accepted live artifacts across the Story Studio chain.                                |
| Open   | Preview live smoke is blocked by Vercel Authentication 401.               | PR preview protection returns Vercel auth before the app action; use post-merge production smoke for public proof or configure a preview bypass before relying on preview smoke.                  |
| Open   | Preview browser smoke is blocked by Vercel Authentication 401.            | `BROWSER_SMOKE_URL=https://pocketfm-contest-forge-2yb69ulcf-phazzies-projects.vercel.app npm run test:browser` reached Vercel auth instead of the app; use local UI smoke or production smoke.    |
| Done   | Live Grok adapter is not wired into UI/server action.                     | Added server-only `runLiveStudio` action, preserves submitted input, and renders live Story Studio artifacts and failures.                                                                        |
| Done   | GitHub Actions warns about Node 20 action runtime.                        | Updated pinned `actions/checkout` and `actions/setup-node` refs to Node 24-compatible releases.                                                                                                   |
| Done   | Deployed live smoke was blocked by SvelteKit CSRF.                        | Smoke script now sends the target deployment as `Origin` while keeping the action header and access-code gate.                                                                                    |
| Done   | Local live smoke could false-pass on Node 23.                             | Smoke script now falls back to resolved entrypoint detection when `import.meta.main` is unavailable.                                                                                              |
| Done   | Live smoke client timed out before provider timeout.                      | Smoke script timeout now exceeds the xAI provider timeout so failures come from the live action path.                                                                                             |
| Done   | Prompt subject text could leak user input into system instructions.       | Names now stay in the JSON input block, with static system guidance and regression coverage for adversarial protagonist text.                                                                     |
| Done   | Production route still centers fixture/demo forge output.                 | Production load now returns locked `StoryStudioRun` artifacts and the page no longer imports `createDefaultForge()` or renders deterministic forge prose.                                         |
| Done   | Story Studio runner is not wired into the route yet.                      | Main form action now calls `RunLiveStoryStudio` through `runLiveStudio`; failed or locked artifacts stay visible instead of falling back.                                                         |
| Done   | Contest freshness is invisible in production.                             | `ContestBrief` now carries source freshness metadata and the route renders status, retrieved date, stale-after date, and warnings.                                                                |

## Architecture Review Notes

| Status | Item                                                             | Finding                                                                                                                                                     | Follow-Up                                                                                               |
| ------ | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Done   | Provider diagnostics do not belong inside module fixtures.       | Kept provider orchestration in `LiveModuleExecutor` instead of expanding module `run()` implementations.                                                    | Real adapters should implement `StoryModuleProvider` under `src/lib/adapters`.                          |
| Done   | JSON repair policy needed a single owner.                        | Live executor owns exactly one balanced-object repair attempt and records `repairAttempts` in provenance.                                                   | Do not duplicate repair policy inside modules or provider adapters.                                     |
| Done   | Review found live boundary acceptance loopholes.                 | Markdown-fenced JSON, blocked provider provenance, hung provider timeout, slug leakage, and unsupported module gates now have tests.                        | Keep adding review-derived cases before introducing the real Grok adapter.                              |
| Done   | Provider adapter should not own JSON repair or acceptance.       | xAI adapter returns raw model text and diagnostics only; `LiveModuleExecutor` still owns repair, schema validation, prose quality, and provenance.          | Keep provider adapters as transport code, not story-module policy.                                      |
| Done   | Live module enablement needed module-specific gate ownership.    | `LiveModuleExecutor` now uses `LiveModuleQualityGateRegistry`; unsupported modules fail before provider calls until a review builder is registered.         | Add one registry entry per module only after prompt, schema, and quality-gate tests exist.              |
| Done   | Public demo needs abuse control before paid AI calls.            | `runLiveStudio` requires `STORY_AI_ACCESS_CODE` and uses a small per-client in-memory rate limit before calling the provider.                               | Replace with durable auth/rate limiting before broader public usage.                                    |
| Done   | Review found live demo polish issues.                            | Expired in-memory rate buckets now prune once the demo map grows, and failed live Grok panels use the same failure styling as failed modules.               | Keep temporary demo controls small until durable auth is chosen.                                        |
| Done   | Review found live access quota loopholes.                        | Failed access-code attempts are rate-limited, paid-call quota is consumed after request validation, and stale live output clears when inputs edit.          | Keep access and quota checks separate in future auth work.                                              |
| Open   | Prose quality gate is deterministic and intentionally crude.     | It blocks obvious generic/prose-invalid output, but it is not a substitute for model critique or taste.                                                     | Add AI council critique after Grok adapter exists; keep deterministic gate as minimum acceptance floor. |
| Done   | Production contract needed a truthful locked-artifact state.     | `StoryStudioRun` now represents accepted, failed, and locked artifacts separately, so incomplete modules do not need fixture prose in production.           | Render this contract in the production route once UI work begins.                                       |
| Done   | Binge Debt Ledger needed a real live path.                       | The module now has provider prompt/input builders, a quality gate registry entry, and live execution from accepted cold-open output.                        | Continue with `cliffhanger-futures` before changing the UI.                                             |
| Done   | Cliffhanger Futures needed a real live path.                     | The module now has provider prompt/input builders, a quality gate registry entry, and live execution from accepted cold-open plus debt-ledger output.       | Continue with `trope-mutation-lab` before changing the UI.                                              |
| Done   | Trope Mutation Lab needed a real live path.                      | The module now has a stronger output contract, provider prompt/input builders, a quality gate registry entry, and live execution from accepted artifacts.   | Continue with `council-review` before changing the UI.                                                  |
| Done   | Council Review needed to be a real module, not a static runbook. | `council-review` is registered with six required roles, provider prompt/input builders, quality gate coverage, and live execution after accepted artifacts. | Continue with removing production route dependence on fixture/demo forge output.                        |
| Done   | Production route depended on fixture/demo forge output.          | The route now imports the pure default request, creates an initial locked Story Studio run, and submits live work through `RunLiveStoryStudio`.             | Continue with component extraction toward `DESIGN.md`.                                                  |
| Done   | Contest freshness needed an explicit contract.                   | `ContestBrief.freshness` now provides source, retrieved date, stale-after date, and warnings; `StoryStudioRun.contestFreshness` derives fresh/stale status. | Add automated research refresh in a separate plan before submission-critical use.                       |
| Done   | Story Studio live runner needed selected-mechanism discipline.   | The runner now treats deselected live mechanisms as locked artifacts before calling the provider, reducing spend and avoiding hidden module execution.      | Keep future modules behind their own mechanism or explicit product gate.                                |
| Done   | Story Studio timeout budget needed to match deployment shape.    | The Vercel adapter now declares a 300-second max duration, while route-level xAI/executor timeouts bound each sequential module call.                       | Longer-term public use should move multi-step runs to a durable job model.                              |
| Done   | First full-chain production smoke proved 45s was too tight.      | The deployed route failed closed with `PROVIDER_TIMEOUT` for `cold-open-lab` instead of fixture fallback.                                                   | Re-run production smoke after the 55/56-second route budget deploys.                                    |
| Done   | Timeout guard needed a graceful near-deadline state.             | PR review caught that five near-timeout calls could still leave too little platform overhead.                                                               | `RunLiveStoryStudio` now checks remaining request budget before starting each provider call.            |
| Done   | Trope Mutation prompt needed to mirror its quality gate.         | Production smoke failed closed because Grok returned `episodePressure` without repeatable cost-bearing pressure.                                            | `trope-mutation-lab.v2` now explicitly requires repeat cues and concrete cost words per pressure item.  |
| Done   | Cliffhanger warning prompt needed to mirror its quality gate.    | Production smoke failed closed because two `payoffWarning` fields described volatility without audience-frustration risk.                                   | `cliffhanger-futures.v2` now requires and gates explicit audience-risk warning prefixes.                |
| Done   | Trope Mutation rule prompt needed to mirror its quality gate.    | Production smoke failed closed because a `mutationRule` lacked a visible inversion, subversion, or rule-change cue.                                         | `trope-mutation-lab.v4` now requires explicit rule-change cue words and rejects weak conjunction cues.  |

## Tracking Targets

Future implementation should track:

- contest brief source, retrieval date, deadline, prize, rules, eligibility, prompt;
- story seed, protagonist, antagonist, desire, taboo, genre lane;
- episode beat maps, cliffhangers, payoff windows, and debt age;
- AI council prompts, model names, outputs, failures, and accepted changes;
- prose review scores and rejection reasons;
- decisions made by the writer versus suggestions made by AI.
